rl:
  backend: sb3_dqn
  checkpoint_name: best_model.zip
  learning_rate: 0.0003
  gamma: 0.98
  wandb_log_interval: 250
  evaluation_episodes: 12
  deterministic_eval: true
  sb3:
    total_timesteps: 120000
    buffer_size: 50000
    learning_starts: 2000
    batch_size: 128
    tau: 1.0
    target_update_interval: 1000
